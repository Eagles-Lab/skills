interface:
  display_name: "Security Resume Analyzer"
  short_description: "Campus security analysis with authorization audit"
  default_prompt: "Use $security-resume-analyzer to map, source-audit, and authorization-check this campus security resume, run deterministic scoring, then publish one cited private report and ten grounded interview questions."
