interface:
  display_name: "Security Resume Analyzer"
  short_description: "Campus security report with cited local enhancement"
  default_prompt: "Use $security-resume-analyzer to map and score this campus security resume, append locally grounded cited guidance to the unchanged deterministic report, generate ten interview questions, then validate and atomically publish the private enriched run."
