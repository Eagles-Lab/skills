interface:
  display_name: "Campus SRE Resume Analyzer"
  short_description: "Campus SRE analysis with grounded source audit"
  default_prompt: "Use $sre-resume-analyzer to map and source-audit this campus SRE resume, run deterministic scoring, then publish one cited private report and ten grounded interview questions."
