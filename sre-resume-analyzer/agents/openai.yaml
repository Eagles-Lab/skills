interface:
  display_name: "Campus SRE Resume Analyzer"
  short_description: "Campus SRE report with cited personalized guidance"
  default_prompt: "Use $sre-resume-analyzer to map and score this campus SRE resume, add locally grounded cited guidance, generate ten interview questions, then validate and atomically publish one unified private suggestions report with its version footer last."
