interface:
  display_name: "Campus SRE Resume Analyzer"
  short_description: "Campus SRE scoring with cited local guidance"
  default_prompt: "Use $sre-resume-analyzer to map and score this campus SRE resume, generate locally grounded cited guidance and ten interview questions, then validate and atomically publish the private enriched run."
